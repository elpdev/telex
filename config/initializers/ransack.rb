Ransack.configure do |config|
  # Change default search parameter key from :q to something else
  # config.search_key = :q

  # Raise errors when unknown predicates or attributes are used
  # config.ignore_unknown_conditions = false
end
