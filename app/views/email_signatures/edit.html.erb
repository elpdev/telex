<% content_for :title, "Telex // edit signature" %>
<% content_for :main_class, "m-0 p-0 w-full min-h-screen bg-bg" %>

<%= render "shared/settings_shell", active: :email_signatures, title: "Edit Signature" do %>
  <%= render Frame::Component.new(title: "Edit signature", variant: :hairline, padding: :lg) do %>
    <%= render "form", email_signature: @email_signature %>
  <% end %>
<% end %>
