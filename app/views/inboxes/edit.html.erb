<% content_for :title, "Telex // edit inbox" %>
<% content_for :main_class, "m-0 p-0 w-full min-h-screen bg-bg" %>
<%= render "shared/settings_shell", active: :domains, title: "Edit Inbox" do %>
  <%= render Frame::Component.new(title: "Modify inbox routing", variant: :hairline, padding: :lg) do %>
    <%= render "form", domain: @domain, inbox: @inbox %>
  <% end %>
<% end %>
