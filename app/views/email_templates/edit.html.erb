<% content_for :title, "Telex // edit template" %>
<% content_for :main_class, "m-0 p-0 w-full min-h-screen bg-bg" %>

<%= render "shared/settings_shell", active: :email_templates, title: "Edit Template" do %>
  <%= render Frame::Component.new(title: "Edit template", variant: :hairline, padding: :lg) do %>
    <%= render "form", email_template: @email_template %>
  <% end %>
<% end %>
