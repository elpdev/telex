<% content_for :title, "Telex // password update" %>
<% content_for :main_class, "m-0 p-0 w-full" %>

<div class="relative flex min-h-screen w-full items-center justify-center bg-bg px-6 py-16 text-phosphor">
  <div class="grid-bg pointer-events-none absolute inset-0 opacity-40"></div>

  <div class="relative z-10 w-full max-w-md">
    <%= link_to root_path, class: "mb-10 flex items-center justify-center hover:opacity-90" do %>
      <%= render Logo::Component.new(size: :md) %>
    <% end %>

    <div class="border border-hairline bg-bg-2">
      <div class="border-b border-hairline px-5 py-3 font-mono text-[0.65rem] uppercase tracking-widest text-phosphor-dim">
        [ AUTH :: SET NEW PASSPHRASE ]
      </div>
      <div class="px-5 py-6">
        <p class="font-mono text-[0.65rem] uppercase tracking-[0.3em] text-phosphor-dim">
          &gt; token accepted<span class="blink text-amber">_</span>
        </p>
        <h1 class="mt-3 font-display text-3xl text-phosphor glow-phosphor">UPDATE CREDENTIALS</h1>

        <%= form_with url: password_path(params[:token]), method: :put, class: "mt-6 space-y-5 font-mono" do |form| %>
          <div>
            <label class="block text-[0.65rem] uppercase tracking-widest text-phosphor-dim">New passphrase</label>
            <%= form.password_field :password, required: true, autocomplete: "new-password",
                placeholder: "************", maxlength: 72,
                class: "mt-2 block w-full border border-hairline bg-bg px-3 py-2 text-sm text-phosphor placeholder:text-phosphor-faint focus:border-phosphor focus:outline-none" %>
          </div>
          <div>
            <label class="block text-[0.65rem] uppercase tracking-widest text-phosphor-dim">Repeat</label>
            <%= form.password_field :password_confirmation, required: true, autocomplete: "new-password",
                placeholder: "************", maxlength: 72,
                class: "mt-2 block w-full border border-hairline bg-bg px-3 py-2 text-sm text-phosphor placeholder:text-phosphor-faint focus:border-phosphor focus:outline-none" %>
          </div>

          <div class="pt-2">
            <%= form.submit "[ > COMMIT ]",
                class: "cursor-pointer border border-amber bg-amber/5 px-4 py-2 text-[0.7rem] uppercase tracking-widest text-amber hover:glow-amber" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
