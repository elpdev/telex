<% content_for :title, "Telex // new domain" %>
<% content_for :main_class, "m-0 p-0 w-full min-h-screen bg-bg" %>
<%= render "shared/settings_shell", active: :domains, title: "New Domain" do %>
  <%= render Frame::Component.new(title: "Provision domain", variant: :hairline, padding: :lg) do %>
    <%= render "form", domain: @domain %>
  <% end %>
<% end %>
